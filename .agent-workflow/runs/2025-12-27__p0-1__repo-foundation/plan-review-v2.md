---
RUN_ID: 2025-12-27__p0-1__repo-foundation
VERSION: v2
TARGET: Phase 0 → Items 0.1-0.3 (Repository Setup, Project Structure, Development Tooling)
INPUTS:
  - .agent-workflow/runs/2025-12-27__p0-1__repo-foundation/plan-v2.md
  - docs/OPUS_REBUILD_FRAME_COMPARE/10-agent-master-checklist.md
  - docs/OPUS_REBUILD_FRAME_COMPARE/11-agent-workflow.md
  - docs/OPUS_REBUILD_FRAME_COMPARE/05-implementation/testing-strategy.md
OUTPUTS:
  - .agent-workflow/runs/2025-12-27__p0-1__repo-foundation/plan-review-v2.md
---

# Plan Review Report: Repository Foundation (Phase 0.1-0.3)

## Verdict: CHANGES REQUIRED

## Review Summary
**Reviewer:** Plan Review Agent
**Date:** 2025-12-27
**Plan Reference:** .agent-workflow/runs/2025-12-27__p0-1__repo-foundation/plan-v2.md

## Checklist Results

| # | Check | Result | Notes |
|---|-------|--------|-------|
| 1 | Scope | PASS | Scope is explicitly Phase 0.1–0.3 with an out-of-scope list. |
| 2 | Dependencies | PASS | Tooling + runtime deps are enumerated; no internal deps required for this slice. |
| 3 | File List | FAIL | Verification step implies creating a lockfile, but `uv.lock` is not listed as an output and plan also says “No additional files”. This leaves ambiguity about whether `uv.lock` is expected/committed. |
| 4 | Contract Impact | PASS | Explicit “NO”; no contract regen gates required. |
| 5 | Types Complete | PASS | Public signatures are concrete and typed; no `Any` introduced. |
| 6 | Tests Complete | PASS | `tests/e2e/test_cli_version.py` is specified with deterministic assertions. |
| 7 | Verification Complete | FAIL | `uv sync --group dev --frozen` cannot “create lockfile if missing”; plan includes a conditional fallback in notes rather than an explicit, single verification flow. Also, acceptance criteria reference `--frozen` as the primary path without specifying lockfile creation/commit expectations. |
| 8 | Decision-Minimizing | FAIL | Lockfile handling (whether to generate, commit, and how to validate) remains a decision point for the Coding Agent. |
| 9 | Determinism Defined | N/A | No randomness in this slice beyond deterministic CLI output. |

## Additional Quality Checks

- Error Codes: OK (no new errors in scope)
- Failure Modes: OK (explicit prerequisite checks and stop conditions added)
- Derived Outputs: Issue — `uv.lock` is a derived artifact from `uv sync` and must be explicitly listed as generated output and treated as “do not hand-edit”.
- Rollback Guidance: OK (explicit “do not patch around failures” guidance is present)

## Implementation Agent Decision Points Remaining

Implementation Agent Decision Points Remaining:
1. Whether `uv.lock` is expected to be created and committed in this run.
2. Which exact command sequence to use when `uv.lock` is absent while still complying with the Command Canon (SSOT).
3. Whether/how to verify that `uv.lock` is stable after generation (e.g., second `--frozen` sync, `git status` check).

## Concrete Edits Required (for plan-v3.md)

1. **Make lockfile handling explicit and add it to the file list**
   - Section: `Files to Create/Modify`, `Notes for Coding Agent`, `Verification Commands`, `Acceptance Criteria`
   - Problem: The plan both (a) uses `--frozen` and (b) claims lockfile creation, but does not list `uv.lock` as an output and forbids “additional files”.
   - Required Change:
     - Add `uv.lock` to the file list as `[NEW | GENERATED]` and state: “Generated by `uv`; do not edit by hand; commit the generated file.”
     - Update “No additional files” guidance to explicitly allow `uv.lock` (and only `uv.lock`) as a generated artifact for this run.

2. **Replace the conditional lockfile note with a single, deterministic verification flow**
   - Section: `Verification Commands`
   - Problem: The plan forces the Coding Agent to choose between the listed commands and the “Lockfile note” fallback.
   - Required Change: Specify the exact verification flow as one sequence:
     1) `uv sync --group dev` (generates `uv.lock` if absent) — pass: exit 0
     2) `uv sync --group dev --frozen` — pass: exit 0
     3) Continue with `.venv/bin/pyright --warnings`, `.venv/bin/ruff check .`, `.venv/bin/pytest -q`, and the `uv run --no-sync ...` + run validators as currently specified.
     - If you require proving lock stability, add an explicit check (e.g., `git status --porcelain` must show no changes after the `--frozen` sync) and include it as a verification step with pass criteria.

3. **Ensure DECISIONS dating rule is followed without guessing**
   - Section: `docs/DECISIONS.md` content
   - Problem: Plan hardcodes `2025-12-27` inside an “exact content” block. This will be incorrect if implementation happens on a different UTC date.
   - Required Change: Replace the hardcoded date in the entry header with an explicit instruction: run `date -u +%Y-%m-%d` and use that value in the `## <DATE> — Phase 0 Foundation Decisions` heading.

## Ready for Implementation

Return to Planning Agent for revision. Next version: `plan-v3.md`.

## NEXT AGENT PROMPT (COPY/PASTE)

You are the Planning Agent for Frame Compare 2.0.

## RUN_ID
2025-12-27__p0-1__repo-foundation

## Revision Required
Read file: .agent-workflow/runs/2025-12-27__p0-1__repo-foundation/plan-review-v2.md
This report contains specific changes required for the plan.

## Previous Plan
Read file: .agent-workflow/runs/2025-12-27__p0-1__repo-foundation/plan-v2.md

## Your Task
Address all items marked FAIL in the plan review. Create a revised plan.

## Output
Write file: .agent-workflow/runs/2025-12-27__p0-1__repo-foundation/plan-v3.md
