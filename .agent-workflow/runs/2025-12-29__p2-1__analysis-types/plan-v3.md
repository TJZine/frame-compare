---
RUN_ID: 2025-12-29__p2-1__analysis-types
VERSION: v3
TARGET: Phase 2 → Item 2.1
INPUTS:
  - .agent-workflow/runs/2025-12-29__p2-1__analysis-types/plan-v2.md
  - .agent-workflow/runs/2025-12-29__p2-1__analysis-types/plan-review-v2.md
  - docs/OPUS_REBUILD_FRAME_COMPARE/05-implementation/module-specs/analysis-module.md
OUTPUTS:
  - .agent-workflow/runs/2025-12-29__p2-1__analysis-types/plan-v3.md
---

# Implementation Plan: Analysis Types

## Changes Since plan-v2

1. **Removed `calculate_metrics` anchor:** Not in scope for types-only slice.
2. **Added explicit per-test assertions:** Each test now specifies exact expected values.
3. **Minimal importlinter.ini edit:** Insert single line, preserve existing structure.

## Context

**Phase:** 2
**Module:** `frame_compare.analysis`
**Spec Reference:** `docs/OPUS_REBUILD_FRAME_COMPARE/05-implementation/module-specs/analysis-module.md`
**Dependencies:** Phase 1 ✅

## Scope

- [ ] Create `src/frame_compare/analysis/types.py` with frozen dataclasses
- [ ] Create `src/frame_compare/analysis/__init__.py` with exports
- [ ] Write unit tests in `tests/analysis/test_types.py`
- [ ] Update `importlinter.ini` (minimal edit)
- [ ] Update `docs/DECISIONS.md`

Out of scope: `metrics.py`, `selection.py`, `cache_io.py`, VapourSynth.

## Contract Impact

**Contracts touched:** NO

## Spec Anchors (SSOT)

- `docs/OPUS_REBUILD_FRAME_COMPARE/05-implementation/module-specs/analysis-module.md`:
  - Section: "2.1 FrameMetrics"
  - Section: "2.2 FrameSelection"
  - Section: "2.3 CacheLoadResult"

## Public API (types-only slice)

This is a types-only slice. The following dataclasses are defined in SSOT Section 2 and must be copied exactly:

1. **ClipIdentity** — `@dataclass(frozen=True, slots=True)` with fields: path, size, mtime, sha1
2. **MetricsMetadata** — `@dataclass(frozen=True, slots=True)` with fields: frame_count, fps, config_fingerprint, clips, version
3. **FrameMetrics** — `@dataclass(frozen=True, slots=True)` with fields: luminance, motion, metadata
4. **SelectionBreakdown** — `@dataclass(frozen=True, slots=True)` with fields: quantile_dark, quantile_bright, motion, random
5. **FrameSelection** — `@dataclass(frozen=True, slots=True)` with fields: frames, mode, seed, breakdown
6. **CacheLoadResult** — `@dataclass(frozen=True, slots=True)` with fields: success, metrics, reason

**Constructor signature (for validator, types are auto-generated by dataclass):**

- `ClipIdentity(path: str, size: int, mtime: float, sha1: str | None = None) -> ClipIdentity`

## Files to Create/Modify

### 1. `src/frame_compare/analysis/__init__.py` (NEW)

Export all public types from `types.py`.

### 2. `src/frame_compare/analysis/types.py` (NEW)

Copy dataclass definitions exactly from SSOT Section 2 code blocks.

### 3. `tests/analysis/__init__.py` (NEW)

Empty marker file.

### 4. `tests/analysis/test_types.py` (NEW)

**Tests with explicit assertions:**

| Test | Assertion |
|------|-----------|
| `test_clip_identity_creation` | `ci = ClipIdentity(path="a.mkv", size=100, mtime=1.0, sha1="abc"); assert ci.path == "a.mkv" and ci.size == 100 and ci.mtime == 1.0 and ci.sha1 == "abc"` |
| `test_clip_identity_optional_sha1` | `ci = ClipIdentity(path="a.mkv", size=100, mtime=1.0); assert ci.sha1 is None` |
| `test_clip_identity_frozen` | `ci = ClipIdentity(...); ci.path = "x"` raises `dataclasses.FrozenInstanceError` |
| `test_metrics_metadata_creation` | `mm = MetricsMetadata(frame_count=100, fps=Fraction(24), config_fingerprint="fp", clips=[]); assert mm.frame_count == 100` |
| `test_metrics_metadata_default_version` | `mm = MetricsMetadata(...); assert mm.version == 2` |
| `test_frame_metrics_creation` | `fm = FrameMetrics(luminance=[0.5], motion=[0.1], metadata=mm); assert fm.luminance == [0.5]` |
| `test_selection_breakdown_creation` | `sb = SelectionBreakdown(quantile_dark=[1], quantile_bright=[2], motion=[3], random=[4]); assert sb.motion == [3]` |
| `test_frame_selection_creation` | `fs = FrameSelection(frames=[1,2], mode=SelectionMode.MIXED, seed=42, breakdown=sb); assert fs.mode is SelectionMode.MIXED` |
| `test_cache_load_result_success` | `clr = CacheLoadResult(success=True, metrics=fm); assert clr.success is True and clr.metrics is fm` |
| `test_cache_load_result_failure` | `clr = CacheLoadResult(success=False, reason="not_found"); assert clr.success is False and clr.metrics is None and clr.reason == "not_found"` |

### 5. `importlinter.ini` (MODIFY)

**Minimal edit:** Insert single new line `frame_compare.analysis` immediately after `frame_compare.cli_entry` in the existing `layers =` list. Do NOT change `name = Layered Architecture` or remove existing comments.

### 6. `docs/DECISIONS.md` (MODIFY)

Append:

```markdown
## 2025-12-29__p2-1__analysis-types

- **Scope:** Analysis module types
- **SSOT edits:** analysis-module.md updated to `@dataclass(frozen=True, slots=True)`
- **Import layers:** Added `frame_compare.analysis` after `cli_entry`
```

### 7. `CHANGELOG.md` (MODIFY)

Under `## [Unreleased]`:

```markdown
### Added
- Analysis module types: `ClipIdentity`, `MetricsMetadata`, `FrameMetrics`, `SelectionBreakdown`, `FrameSelection`, `CacheLoadResult`
```

## Acceptance Criteria

- [ ] All dataclasses use `@dataclass(frozen=True, slots=True)`
- [ ] Assignment to frozen field raises `FrozenInstanceError`
- [ ] `lint-imports` passes
- [ ] pyright shows 0 errors

## Verification Commands

```bash
.venv/bin/pyright --warnings src/frame_compare/analysis/
.venv/bin/ruff check src/frame_compare/analysis/
.venv/bin/pytest -v tests/analysis/
.venv/bin/pyright --warnings
UV_CACHE_DIR=./.uv_cache uv run --no-sync lint-imports --config importlinter.ini
```

**Pass criteria:** All exit 0.

## Notes for Coding Agent

1. **Frozen dataclasses:** Copy SSOT exactly with `@dataclass(frozen=True, slots=True)`.
2. **SelectionMode:** Import from `frame_compare.config`.
3. **importlinter.ini:** Minimal edit only - insert one line.
4. **Rollback:** If import contracts fail, STOP and return to Planning.

---

## NEXT AGENT PROMPT (COPY/PASTE)

You are the Plan Review Agent for Frame Compare 2.0.

## RUN_ID

2025-12-29__p2-1__analysis-types

## Plan to Review

Read file: .agent-workflow/runs/2025-12-29__p2-1__analysis-types/plan-v3.md

## Context Files to Read

1. Read file: docs/OPUS_REBUILD_FRAME_COMPARE/05-implementation/module-specs/analysis-module.md
2. Read file: docs/OPUS_REBUILD_FRAME_COMPARE/11-agent-workflow.md

## Your Task

Validate the plan using the 9-point checklist. Produce a Plan Review Report.

## Output

Write file: .agent-workflow/runs/2025-12-29__p2-1__analysis-types/plan-review-v3.md
