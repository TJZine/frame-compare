---
name: model-selection
description: "Use when choosing a model or reasoning effort for a Frame Compare task or handoff."
---

# model-selection (Claude Code entry)

Canonical skill: [`.agents/skills/model-selection/SKILL.md`](../../../.agents/skills/model-selection/SKILL.md), shared with Codex.
This file is only the Claude Code discovery entry. Do not copy the skill body here.

Before acting, read `.agents/skills/model-selection/SKILL.md` from the repository root in full and follow it as this skill's instructions.
Resolve its relative links from `.agents/skills/model-selection/`.

Claude Code runtime: the role roster and routing above are unchanged, but the authority for each role's model, effort, and tools is `.claude/agents/<role>.md` (same role names). The OpenAI model and sandbox settings in `.codex/config.toml` and `.codex/agents/*.toml` configure Codex only.
