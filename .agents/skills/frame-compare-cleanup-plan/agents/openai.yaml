interface:
  display_name: "Frame Compare Cleanup Plan"
  short_description: "Launch the cleanup/refactor planner workflow"
  default_prompt: "Use $frame-compare-cleanup-plan for this task."
policy:
  allow_implicit_invocation: false
