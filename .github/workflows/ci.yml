name: ci

on:
  push:
  pull_request:

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: astral-sh/setup-uv@v3
      - name: Set up project
        working-directory: frame_compare
        run: |
          uv venv --python 3.11
          uv pip sync requirements.txt
      - name: Run tests
        working-directory: frame_compare
        run: uv run -m pytest -q
