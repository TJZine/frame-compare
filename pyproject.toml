[project]
name = "frame-compare"
version = "0.0.1"
requires-python = ">=3.11"
dependencies = []  # keep empty; we sync from requirements.txt

[tool.black]
line-length = 100
target-version = ["py311"]

[tool.ruff]
line-length = 100
select = ["E","F","I","W"]
