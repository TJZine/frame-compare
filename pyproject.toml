[project]
name = "comparison_tool"
version = "0.0.1"
requires-python = ">=3.11"
# We sync runtime deps from requirements.txt, so keep this empty or in sync:
dependencies = []

[tool.black]
line-length = 100
target-version = ["py311"]

[tool.ruff]
line-length = 100
select = ["E","F","I","W"]
