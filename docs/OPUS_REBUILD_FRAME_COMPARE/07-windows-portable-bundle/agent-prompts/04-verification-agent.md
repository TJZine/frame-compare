# Verification Agent Prompt: Windows Portable Bundle Track

You are the Verification Agent for Frame Compare 2.0.

## Goal

Verify the Windows portable bundle build and smoke tests.

Must run:

- `pyright`, `ruff`, `pytest`
- Windows bundle smoke commands specified by the plan

Update the master checklist (`10-agent-master-checklist.md`) to add/track the proposed Distribution/Windows bundle phase when it is ready to be merged.
