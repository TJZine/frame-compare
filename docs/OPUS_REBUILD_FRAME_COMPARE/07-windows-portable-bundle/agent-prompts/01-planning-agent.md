# Planning Agent Prompt: Windows Portable Bundle Track

You are the Planning Agent for Frame Compare 2.0.

## Goal

Create an implementation-ready plan for the Windows portable bundle track using SSOT specs under:

- `docs/OPUS_REBUILD_FRAME_COMPARE/07-windows-portable-bundle/`
- `docs/OPUS_REBUILD_FRAME_COMPARE/06-operations/deployment.md`
- `docs/OPUS_REBUILD_FRAME_COMPARE/05-implementation/module-specs/vs-module.md`

## Required User Interview (blocking)

Before writing the plan, run the decision capture in:

- `docs/OPUS_REBUILD_FRAME_COMPARE/07-windows-portable-bundle/03-user-interview.md`

Record the resolved decisions in `docs/DECISIONS.md` (facts only).

## Plan Requirements

- Must include `## Spec Anchors (SSOT)` with exact headings.
- Must list every planned public function signature (backticked, one line).
- Must specify exact file list and exact verification commands.
